query Albums($artistId: ID, $limit: Int = 100, $offset: Int = 0) {
  albums(artistId: $artistId, limit: $limit, offset: $offset) {
    totalCount
    items {
      id
      title
      coverUrl
      artist {
        id
        name
      }
    }
  }
}
