query StartDownloadSearch($query: String!) {
  startDownloadSearch(query: $query) {
    id
    query
    isComplete
    fileCount
    responseCount
    peers {
      peer
      hasFreeUploadSlot
      uploadSpeed
      queueLength
      folders {
        name
        files {
          filename
          size
          extension
          bitRate
          bitDepth
          sampleRate
          isLocked
        }
      }
    }
  }
}
