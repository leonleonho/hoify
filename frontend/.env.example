# GraphQL API endpoint.
# On web: localhost is rewritten — same origin on 80/443 (reverse proxy),
# or page hostname + this port on LAN/dev (e.g. :3000 UI → :4000 API).
EXPO_PUBLIC_API_URL=http://localhost:4000/graphql
